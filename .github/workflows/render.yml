name: 🎬 Render Remotion Video

on:
  # Only manual triggers — NO push trigger (that caused double runs)
  workflow_dispatch:
    inputs:
      composition_id:
        description: 'Single composition ID'
        required: false
        default: ''
      compositions_json:
        description: 'Bulk JSON: [{"id":"CompA","file":"comp0"},{"id":"CompB","file":"comp1"}]'
        required: false
        default: ''

jobs:
  # ── Build matrix from inputs ───────────────────────────────────────────────
  prepare:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set.outputs.matrix }}
    steps:
      - id: set
        run: |
          BULK='${{ github.event.inputs.compositions_json }}'
          SINGLE='${{ github.event.inputs.composition_id }}'
          if [ -n "$BULK" ] && [ "$BULK" != "null" ] && [ "$BULK" != "" ]; then
            echo "matrix={\"include\":$(echo $BULK)}" >> $GITHUB_OUTPUT
          elif [ -n "$SINGLE" ] && [ "$SINGLE" != "" ]; then
            echo "matrix={\"include\":[{\"id\":\"$SINGLE\",\"file\":\"composition\"}]}" >> $GITHUB_OUTPUT
          else
            echo "matrix={\"include\":[{\"id\":\"CybersecurityNetworkMap\",\"file\":\"composition\"}]}" >> $GITHUB_OUTPUT
          fi

  # ── Render — parallel per matrix entry ────────────────────────────────────
  render:
    needs: prepare
    name: Render ${{ matrix.id }}
    runs-on: ubuntu-latest
    timeout-minutes: 120
    strategy:
      matrix: ${{ fromJson(needs.prepare.outputs.matrix) }}
      fail-fast: false

    steps:
      - uses: actions/checkout@main
      - uses: actions/setup-node@main
        with:
          node-version: '20'
      - run: npm i

      - name: 🎬 Render ${{ matrix.id }}
        run: |
          mkdir -p out
          npx remotion render \
            "${{ matrix.id }}" \
            "out/${{ matrix.id }}.mp4" \
            --width=3840 \
            --height=2160 \
            --fps=60 \
            --frames=0-899 \
            --codec=h264 \
            --crf=18 \
            --pixel-format=yuv420p \
            --concurrency=2

      - uses: actions/upload-artifact@v4
        with:
          name: "${{ matrix.id }}_4K_60fps"
          path: "out/${{ matrix.id }}.mp4"
          retention-days: 2
